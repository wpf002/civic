services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: civic
      POSTGRES_PASSWORD: civic
      POSTGRES_DB: civic
    ports: ["5433:5432"]   # 5432 is often taken by a native local Postgres
    volumes: ["pgdata:/var/lib/postgresql/data"]
volumes:
  pgdata:
