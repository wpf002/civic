services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: civic
      POSTGRES_PASSWORD: civic
      POSTGRES_DB: civic
    ports: ["55432:5432"]   # 5432-5439 are crowded on dev machines; stay out of that range
    volumes: ["pgdata:/var/lib/postgresql/data"]
volumes:
  pgdata:
